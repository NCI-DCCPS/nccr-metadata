# nccr-metadata
Semantic metadata for the NCCR Data Platform — includes an RDF vocabulary for variable-level data dictionary metadata, DATMM dataset catalog records, SKOS value sets, and tools to generate linked data from the platform's JSON configurations.
